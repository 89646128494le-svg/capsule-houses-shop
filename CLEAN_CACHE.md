# Очистка кэша Next.js

Если вы видите ошибку `SyntaxError: Unexpected end of JSON input` или проблемы с кэшем webpack, это означает, что кэш Next.js поврежден.

## ✅ Быстрое решение

**Остановите dev-сервер (Ctrl+C) и выполните:**

```bash
npm run clean
npm run dev
```

## Альтернативные способы

### Вариант 1: Ручная очистка (Windows PowerShell)

```powershell
Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force node_modules\.cache -ErrorAction SilentlyContinue
npm run dev
```

### Вариант 2: Ручная очистка (Windows CMD)

```cmd
rmdir /s /q .next
rmdir /s /q node_modules\.cache
npm run dev
```

### Вариант 3: Ручная очистка (Mac/Linux)

```bash
rm -rf .next
rm -rf node_modules/.cache
npm run dev
```

### Вариант 4: Полная переустановка (если ничего не помогает)

```bash
# Удалить кэш и зависимости
rm -rf .next
rm -rf node_modules
rm -rf package-lock.json

# Переустановить
npm install
npm run dev
```

## 📝 Примечание

После очистки кэша первый запуск может занять немного больше времени (30-60 секунд), так как Next.js пересоберет все файлы. Это нормально.

## 🔍 Дополнительная информация

Ошибка `SyntaxError: Unexpected end of JSON input` обычно возникает когда:
- Процесс сборки был прерван (Ctrl+C во время компиляции)
- Произошла ошибка при записи кэша
- Файловая система была перегружена

Очистка кэша решает эту проблему в 99% случаев.
