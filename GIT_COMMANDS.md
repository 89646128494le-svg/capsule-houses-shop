# Команды для обновления репозитория

## 📤 Быстрое обновление (все изменения)

```bash
git add .
git commit -m "Обновление: исправления ошибок и улучшения"
git push
```

## 📋 Пошаговое обновление

### 1. Проверить статус изменений
```bash
git status
```

### 2. Добавить все изменения
```bash
git add .
```

Или добавить конкретные файлы:
```bash
git add app/
git add components/
git add lib/
```

### 3. Создать коммит с описанием
```bash
git commit -m "Описание ваших изменений"
```

Примеры сообщений:
- `"Исправлены ошибки линтера и улучшена доступность"`
- `"Добавлена обработка ошибок в формах"`
- `"Обновлен функционал админ-панели"`
- `"Исправлены проблемы с кэшем Next.js"`

### 4. Отправить изменения в репозиторий
```bash
git push
```

Если это первый push в новую ветку:
```bash
git push -u origin main
```
или
```bash
git push -u origin master
```

## 🔄 Полный цикл обновления

```bash
# Проверить статус
git status

# Добавить изменения
git add .

# Закоммитить
git commit -m "Ваше описание изменений"

# Отправить в репозиторий
git push
```

## 📥 Получить последние изменения из репозитория

```bash
git pull
```

## 🌿 Работа с ветками

### Создать новую ветку
```bash
git checkout -b название-ветки
```

### Переключиться на ветку
```bash
git checkout название-ветки
```

### Посмотреть все ветки
```bash
git branch
```

## ⚠️ Важные замечания

1. **Перед push всегда проверяйте статус:**
   ```bash
   git status
   ```

2. **Не коммитьте файлы из `.gitignore`:**
   - `.env.local` (секретные ключи)
   - `.next/` (кэш Next.js)
   - `node_modules/` (зависимости)

3. **Если нужно отменить изменения:**
   ```bash
   # Отменить изменения в файле (до git add)
   git checkout -- имя-файла
   
   # Отменить git add
   git reset HEAD имя-файла
   
   # Отменить последний коммит (сохранить изменения)
   git reset --soft HEAD~1
   ```

4. **Если возникли конфликты при pull:**
   ```bash
   git pull
   # Разрешить конфликты вручную
   git add .
   git commit -m "Разрешение конфликтов"
   git push
   ```

## 🚀 Быстрая команда для копирования

```bash
git add . && git commit -m "Обновление проекта" && git push
```
